services:
  tests-e2e:
    build:
      context: ../tests
    depends_on:
      caddy:
        condition: service_started
    network_mode: "host"
    volumes:
      - ../tests/playwright-report:/tests/playwright-report
      - ../tests/tests:/tests/tests
