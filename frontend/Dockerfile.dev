FROM denoland/deno:2.4.5 AS builder
RUN apt-get update && apt-get install -y curl
WORKDIR /app
EXPOSE 3000
COPY . .

CMD [ "sh", "-c", "deno install --allow-scripts=npm:@tailwindcss/oxide@4.1.11 && deno task dev" ]

HEALTHCHECK CMD curl -f -o /dev/null http://localhost:3000
